const colours = ['red','green','yellow','blue'];
console.log(`Colours in the arrays are: ${colours}`);

const newcolours = colours.push('white','black');

console.log(`Colours after push into the arrays: ${colours}`);